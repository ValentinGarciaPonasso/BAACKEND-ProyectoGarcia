<div>
    <h1>Listado de Productos por db:</h1>
    <div class="bloque">
        {{#each product.docs}}
        <div class="tarjeta">
            <p>Titulo: {{this.title}}</p>
            <p>Precio: {{lookup this 'price'}}</p>
            <p>Stock: {{lookup this 'stock'}}</p>
            <p>Categoría: {{lookup this 'category'}}</p>
            <a href="/products/{{lookup this 'id'}}">Ver Producto</a>
            <button type="submit" id="addToCart" class="addToCart" value={{lookup this 'id'}}>Agregar al carrito</button>
        </div>
        {{/each}}
    </div>
    <div>
        {{#if product.prevPage}}
        <a href={{product.prevLink}}>Página Anterior</a>
        {{/if}}
        {{#if product.nextPage}}
        <a href={{product.nextLink}}>Página Siguiente</a>
        {{/if}}
    </div>
</div>

<script type="text/javascript" src="/js/product.js"></script>